<!--book/indexを引用。Userの一覧-->
<div class="container px-5 px-sm-0">
  <div class="row">
    <!--部分テンプレート_user_infoを呼び出し-->
    <%= render 'users/user_info',{user: @user,book_new: @book_new} %>

<!--ユーザの投稿一覧-->
    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <!--table-hoverでカーソルを置くと色が変わる-->
      <table class="table">
        <thead>
          <tr>
            <th>image</th>
            <th>name</th>
            <th colspan="3"></th>
        </thead>

        <tbody>
          <% @users.each do |user| %>
          <tr>
            <td><%= image_tag user.get_profile_image(100,100) %></td>
            <td><%= user.name %></td>
            <!--該当userの投稿一覧画面のリンクを設定したい-->
            <!--見本ではclassにuser_idにidが入っている-->
            <td><%= link_to "Show",user_path(user.id),class:"user_id" %></td>
          </tr>
          <% end %>
        </tbody>
      </table>

    <!--ページネーション-->
  <%= paginate @users %>
</div>
