<!--ユーザの詳細-->
<div>
  <h3><%= @user.name %></h3>
  <%= image_tag @user.get_profile_image(100,100) %>
</div>

<!--ユーザの投稿一覧-->
<% @books.each do |book| %>
<div>
  <!--bookの画像投稿はないからコメントアウトに-->
  <!--<%= link_to book_path(book.id) do %>-->
    <!--<%= image_tag book.get_image %>-->
  <!--<% end %>-->
  <p><%= image_tag book.user.get_profile_image(100,100) %></p>
  <p><%= book.title %></p>
  <p><%= book.body %></p>
  <p><%= book.user.name %></p>
</div>
<% end %>