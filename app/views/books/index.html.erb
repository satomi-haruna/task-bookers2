<!--カリキュラムではuser/showへ記載になっていた部分-->
<div class="container mx-auto px-sm-0">
  <div class="row">
    <!--部分テンプレート_user_infoを呼び出し-->
    <%= render 'users/user_info',{user: @user,book_new: @book_new} %>

<!--ユーザの投稿一覧-->
    <div class="col-md-7 offset-1 px-3">
      <h2 class="mb-2">Books</h2>
      <!--table-hoverでカーソルを置くと色が変わる-->
      <table class="mb-2 table-hover">
        <thead class="table ">
          <tr>
            <th style="width:10%;"></th>
            <th style="width:30%;">Title</th>
            <th style="width:50%;">Opinion</th>
            <th colspan="3" style=width:10%;></th>
          </tr>
        </thead>

        <tbody>
          <% @books.each do |book| %>
          <tr>
            <!--userの画像は下の一文で表示できている?-->
            <td style="width:10%;" class="p-3"><%= image_tag book.user.get_profile_image(100,100) %></td>
            <!--一覧画面に詳細画面のリンクを設定-->
            <td style="width:30%;" class="p-3"><%= link_to book.title,book_path(book.id) %></td>
            <td style="width:50%;" class="p-3"><%= book.opinion %></td>
            <td colspan="3" style=width:10%; class="p-3"></td>
          </tr>
          <% end %>
        </tbody>
      </table>

      <!--ページネーション-->
      <%= paginate @books %>
    </div>
  </div>
</div>

<!--_list部分テンプレートへ → 部分テンプレートにせずに記述してみる-->
<!--%= render 'list',books: @books %>-->
